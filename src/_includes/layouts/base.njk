<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% if title %}{{ title }} · {{ site.shortTitle }}{% else %}{{ site.title }}{% endif %}</title>
    <meta name="description" content="{{ description or site.description }}" />
    {% if isPreview %}<meta name="robots" content="noindex, nofollow" />{% endif %}
    <link rel="canonical" href="{{ page.url | absUrl }}" />
    <link rel="icon" href="/assets/img/photos/favicon-source.png" type="image/png" />
    <link rel="apple-touch-icon" href="/assets/img/photos/favicon-source.png" />
    <link rel="alternate" type="application/atom+xml" title="{{ site.title }} News" href="/news/feed.xml" />
    <link rel="preload" as="font" type="font/woff2" href="/assets/fonts/eb-garamond-normal-latin.woff2" crossorigin />
    <link rel="preload" as="font" type="font/woff2" href="/assets/fonts/montserrat-normal-latin.woff2" crossorigin />
    <link rel="stylesheet" href="/assets/css/fonts.css" />
    <link rel="stylesheet" href="/assets/css/style.css" />

    <meta property="og:type" content="{{ ogType or 'website' }}" />
    <meta property="og:site_name" content="{{ site.title }}" />
    <meta property="og:title" content="{{ title or site.title }}" />
    <meta property="og:description" content="{{ description or site.description }}" />
    <meta property="og:url" content="{{ page.url | absUrl }}" />
    <meta property="og:locale" content="{{ site.locale }}" />
    <meta name="twitter:card" content="summary_large_image" />

    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Church",
        "name": {{ site.title | dump | safe }},
        "url": {{ site.url | dump | safe }},
        "telephone": {{ site.phone | dump | safe }},
        "email": {{ site.email | dump | safe }},
        "address": {
          "@type": "PostalAddress",
          "streetAddress": {{ site.address.street | dump | safe }},
          "addressLocality": {{ site.address.city | dump | safe }},
          "addressRegion": {{ site.address.region | dump | safe }},
          "postalCode": {{ site.address.postalCode | dump | safe }},
          "addressCountry": {{ site.address.country | dump | safe }}
        }
      }
    </script>
  </head>
  <body class="{% if page.url == '/' %}is-home{% endif %}">
    <a class="skip-link" href="#main">Skip to content</a>
    {% include "partials/header.njk" %}
    <main id="main">{{ content | safe }}</main>
    {% include "partials/footer.njk" %}
    {% if pageScripts %}
      {% for script in pageScripts %}
        <script src="{{ script }}" defer></script>
      {% endfor %}
    {% endif %}
  </body>
</html>
