# Nothing is currently withheld from the build.
#
# The Giving page (src/giving.njk) is published but deliberately left out of the
# main navigation until the parish decides to launch online giving. It currently
# renders an interactive mock of the checkout — no payment is taken. To launch:
# add the Giving entry back to src/_data/navigation.json, repoint the /store
# redirect in src/static/_redirects, and follow docs/giving-setup.md.
